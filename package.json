{
  "name": "source",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "openCypress": "cypress open",
    "runCypress": "cypress run",
    "runTestOnChrome": "cypress run --browser chromer",
    "runTestHeadlessOnChrome": "cypress run --browser chrome --headed"
    
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.1.0"
  }
}
